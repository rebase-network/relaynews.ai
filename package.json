{
  "name": "relaynews-ai",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev:origin": "pnpm --filter @relaynews/origin run dev",
    "dev:web": "pnpm --filter @relaynews/web run dev",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  }
}
